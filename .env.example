# XanoScript Patch Configuration
# Copy this file to .env and adjust paths for your environment

# Path to the xanoscript_vscode_plugin git repository
REPO_PATH="/Users/username/repos/xanoscript_vscode_plugin"

# Path to the XanoScript extension to be patched
# You can override this when running apply-patch.sh by providing the path as argument
EXTENSION_PATH="~/.vscode/extensions/xano.xanoscript-0.0.73"

# Output patch file name (relative to script directory)
PATCH_FILE="api-group-fix.patch"

# Git commit to generate patch from (HEAD = latest commit)
COMMIT="HEAD"
